<form autocomplete="off" onsubmit="event.preventDefault();">
    {{> "dnd5a.trait-list" prefix="choices"}}

    {{#if customPath}}
        <div class="form-group stacked">
            <label>{{localize "DND5A.TraitSelectorSpecial"}}</label>
            <div class="form-fields">
                <input type="text" name="{{customPath}}" value="{{custom}}">
            </div>
        </div>
    {{/if}}

    {{#if bypassesPath}}
        <h3>{{localize "DND5A.DamagePhysicalBypass"}}</h3>
        <p class="note">{{localize "DND5A.DamagePhysicalBypassHint"}}</p>
        {{> "dnd5a.trait-list" choices=bypasses prefix="bypasses"}}
    {{/if}}

    <button type="submit"><i class="far fa-save"></i> {{localize "DND5A.TraitSave"}}</button>
</form>
