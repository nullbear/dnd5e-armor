<div class="flexrow">
    {{~#if displaySelection}}
    <div class="count {{ invalid }}">
        {{{ localize "DND5A.CompendiumBrowser.Selection.Label" summary=summary }}}
    </div>
    <button type="submit">
        {{ localize "DND5A.CompendiumBrowser.Selection.Select" }}
    </button>
    {{/if~}}
</div>
